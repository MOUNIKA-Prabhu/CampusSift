# Configuration Secrets (Keep environment variables safe)
# If GEMINI_API_KEY is provided, the system will generate Gemini-based explainability rationale.
# If omitted, system seamlessly falls back to offline deterministic rule-based NLP explainability engine.
GEMINI_API_KEY=
OPENAI_API_KEY=
